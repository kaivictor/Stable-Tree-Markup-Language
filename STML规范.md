# STML 1.6 语义规范

**Stable Tree Markup Language**
版本：1.6
状态：正式规范
设计目标：输入宽松、输出严格、解析完全确定性、永不崩溃，程序可以根据需要对内容进行检查和转换

---

## 1. 概述

STML 是一种面向人类手写与机器生成的纯文本数据格式。它继承 YAML 的简洁语法，但移除所有隐式类型推断、锚点、别名、复杂多行指示符等特性，并通过严格的容错与确定性规则确保：**任何符合本规范的解析器，对任意输入都会生成完全相同的抽象语法树（AST），且永远不会崩溃**。

---

## 2. 数据模型

STML AST 仅包含以下四种节点类型：

- **Mapping**（映射）：有序的键值对集合，键为字符串，值可为任意节点。
- **Sequence**（列表）：有序的值列表，值可为任意节点。
- **Scalar**（字符串）：纯文本值。
- **Null**（空值）：表示缺失或空。

除显式空值外，**所有值均为字符串**。不存在数字、布尔、日期等自动类型推断。字面量 `true`、`false`、`123`、`1.5` 等全部解析为字符串。

> **空容器约定**：STML 语法中没有显式表示空映射或空序列的方式。当映射条目无子块时，其值视为 `null`；序列条目无内容且无子块时，其值同样视为 `null`。即，空容器在解析时统一映射为 `null`，不做区分。标准输出（§15）亦将 `null` 作为所有空结构的统一表示。

---

## 3. 文档结构

一个 STML 文件包含零个或多个文档，文档之间由 `---` 行分隔。

```
文档1
---
文档2
---
```

**规则**：
- 只有**缩进为 0**（即行首无空格）且去除尾空白后恰好为 `---` 的行才被视为文档分隔符。缩进大于 0 的 `---` 行始终视为普通内容。
- 文件开头或末尾的 `---` 可省略。
- 空文件或全注释文件视为包含**一个空文档，该文档的 AST 为 `null`**。

**解析输出结构**（核心语义）：
- 解析器始终返回一个 Mapping 节点，其单一键为 `"docs"`，值为一个 **Sequence（列表）**，列表元素按顺序为每个文档解析后的 AST 节点。
- **单文档文件**（无 `---` 分隔符）：`{"docs": [{...}]}`（单元素列表）。
- **空文件或全注释文件**：`{"docs": []}`（空列表）。

---

## 4. 词法基础

### 4.1 行与缩进
- 按行解析，行分隔符为 `\n` 或 `\r\n`。
- **缩进仅由空格（U+0020）组成**。制表符（U+0009）不具有缩进语义，始终视为普通内容字符。
- 每行的缩进 = 行首连续空格数。

### 4.2 注释
仅支持**整行注释**。若一行去除前导空格后以 `#` 开头，则该行为注释行，解析时直接丢弃。不支持行尾注释，多行字符串内所有行均不视为注释。**注释行完全透明，不参与缩进边界判断**。一个块是否结束仅取决于有效内容行（非注释行）的缩进。

### 4.3 行分类
去除前导空格后，每行属于以下之一：
- **注释行**（以 `#` 开头，已丢弃）
- **序列条目行**（以 `-` 开头）
- **映射条目行**（非 `-` 开头，可能包含结构冒号或为裸键）
- **多行字符串内容行**（位于 `{…}` 块内部）
- **裸键行**（映射条目中不含合法结构冒号）

---

## 5. 结构冒号扫描算法

解析器**从左到右扫描字符流**，维护一个布尔状态 `in_quote`（初始为假）。按以下规则处理每个字符：

1. 若当前字符为 `\` 且后续还有字符，则**跳过这两个字符**（转义处理），不改变 `in_quote` 状态（即反斜杠及其后字符不参与引号翻转判断）。
2. 若当前字符为 `"`（且未被上一步跳过），则翻转 `in_quote` 状态。
3. 若当前字符为 `:` 且 `in_quote` 为假，则该冒号即为**结构冒号**，算法终止。

若扫描至行尾仍未找到结构冒号，则该行无结构冒号。

---

## 6. 字符串与转义

### 6.1 双引号字符串
双引号包裹的字符串支持以下转义序列（解析时应用）：

| 输入 | 含义 |
|------|------|
| `\"` | 双引号 `"` |
| `\\` | 反斜杠 `\` |
| `\n` | 换行符（U+000A） |
| `\t` | 制表符（U+0009） |
| `\X`（其他） | 保留为 `\X`（反斜杠 + X） |

### 6.2 引号键
若一行以 `"` 开头，解析器优先尝试“引号键”模式：
1. 寻找**第一个**未转义的双引号作为键的闭合（非贪婪）。
2. 该闭合引号后的**紧邻字符必须为 `:`**（不允许空格）。
3. 若满足，则引号内内容（反转义）为键，`:` 之后部分解析值。
4. 否则，放弃引号键，回退为普通映射条目（使用结构冒号算法）。

### 6.3 引号值
值部分（无论映射值还是序列条目内容）若以 `"` 开头，则使用**非贪婪匹配**：寻找**第一个**未转义的双引号作为闭合，中间内容（反转义）为字符串值。若未找到闭合引号，则降级为原始标量（保留开头的 `"` 及全部字符），不报错。

### 6.4 序列条目中的引号值
序列条目内容解析完全复用 §6.3 的引号值规则。

---

## 7. 缩进与块模型

STML 采用**父缩进边界模型**，但区分**映射块**与**序列块**的缩进要求：

- **映射块**：当前行缩进 > 父节点缩进时，该行属于父块；若缩进 ≤ 父节点缩进，则映射块结束。
- **序列块**：当前行以 `-` 开头时，其缩进只需 **≥ 父节点缩进** 即可被视为序列条目（即不强制增加缩进）。序列块结束的条件为：遇到非 `-` 开头的行且该行缩进 ≤ 父节点缩进，或者 `-` 开头的行缩进 < 父节点缩进。

同级映射条目的缩进可不同，只需严格大于父缩进；同级序列条目的缩进也可不同，只需不小于父缩进。

此规则允许常见的列表书写风格，如：
```yaml
键4:
  - 值41
- 值42        # 缩进为 0，仍属于键4 的序列
```

---

## 8. 块类型冻结与冲突恢复

块在出现第一个有效子项（忽略注释）后类型冻结，之后不得混合。

- **映射块**：第一个子项非 `-` 开头。
- **序列块**：第一个子项以 `-` 开头。

若后续出现不符合当前块类型的冲突行，则**立即终止当前块**，并将冲突行**交还给父级上下文重新解析**。冲突行将重新评估为父级的新条目。

---

## 9. 映射（Mapping）规则

### 9.1 条目解析
1. 尝试引号键模式（§6.2）。
2. 使用结构冒号算法（§5）寻找第一个结构冒号。
   - 若找到，冒号前为键（原样保留，不去除尾部空格），冒号后为值的部分。
   - 若未找到冒号，整行为裸键，值为 `null`。
3. 裸键在解析完成后**立即闭合**。对于后续行，解析器会向上查找可容纳的父节点：忽略裸键（因为它不创建子块），从直接父级开始，寻找第一个缩进小于该行缩进且类型兼容的节点，将该行添加为其新条目。

### 9.2 值的解析
冒号后的值部分（去除冒号后紧接的一个可选空格）按顺序判定：
1. 若去除首尾空格后**恰好等于 `{`** 且下一行存在（不论缩进），进入多行字符串模式。
2. 若为空或仅空格，解析后续行为块（映射或序列）。若无有效子块行，则值为 `null`。
3. 若以 `"` 开头，按引号值规则（§6.3）解析；失败则降级为原始标量。
4. 若以 `[` 开头且以 `]` 结尾，按内联列表解析。
5. 其余情况，将值部分**去除首尾空格**后按标量处理（§12）。

若映射条目拥有行内值（非块值），则该节点立即闭合，后续更深缩进的行不属于该节点，而是根据 §9.1 第 3 点的原则向上查找父节点。

---

## 10. 内联列表

格式：`[元素1, 元素2, "元素,3"]`

- 仅一层，不支持嵌套。
- 元素之间用英文逗号 `,` 分隔。
- 引号内元素使用**非贪婪匹配**（第一个未转义 `"`），完全遵循 §6.3，内部逗号不拆分。
- 无引号元素去除首尾空格后作为标量（`null`/`~` 转为空值）。
- 空方括号 `[]` 表示空列表。
- **空元素处理**：两个逗号之间若无任何字符（忽略空格），则该位置元素为空字符串 `""`。尾随逗号不产生额外空元素。
- **格式错误恢复**：若内联列表缺少结尾 `]`，则整个字符串（包括 `[`）降级为原始标量。

---

## 11. 多行字符串

- **起始条件**：映射条目值部分去除首尾空格后**恰好等于 `{`**，且下一行存在。
- 内容：从下一行起，所有行原样保留（包括缩进、空行、转义等），不解析任何结构。
- 结束：遇到一行，去除缩进和尾空白后恰好为 `}`，且该行缩进 ≤ 键所在行缩进。结束行不进入内容。
- EOF 自动闭合：若未遇到合法结束行，则读到文件末尾自动闭合。
- 内部规则：不识别注释、引号、冒号、序列等。

---

## 12. 标量（Scalar）处理

对任意未加引号的字符串内容，按以下流程转换为标量：

1. **去除首尾空格**。
2. 若结果恰好等于 `null` 或 `~`，则返回 **Null**。
3. 否则返回该去除首尾空格后的字符串。

引号包裹的 `"null"` 和 `"~"` 不受此规则影响，因为引号值已在早期阶段处理完毕，直接作为字符串。

---

## 13. 序列（Sequence）规则

### 13.1 条目识别与多键模型

序列由连续的 `-` 开头行组成，但**每个序列条目可以包含多个键值对**。解析器将每个 `-` 行视为一个条目的起点，后续同一缩进级别的非 `-` 行将作为该条目的兄弟键加入同一映射。遇到下一个 `-` 行时，当前条目闭合，开始新条目。

条目核心语义：**每个序列条目解析为一个 Mapping**。即使该条目只包含单键（或仅一个标量值），在逻辑上也视为含一个键值对的映射。

条目缩进只需 **≥ 序列块父节点缩进**。同级序列条目的缩进可不同，只需不小于父缩进。

**条目边界规则**：
- 遇到新的 `-` 行（且缩进 ≥ 父缩进）→ 当前条目闭合，新条目开始。
- 遇到非 `-` 行的 KEY_VAL 或 BARE_KEY（同级缩进）→ 吸收为当前条目的兄弟键。
- 遇到缩进 ≤ 父缩进且非 `-` 的行 → 序列块结束。
- 遇到缩进 < 父缩进的 `-` 行 → 序列块结束。

### 13.2 条目内首个键值对解析

每条目的第一个 `-` 行（去掉 `-` 及随后的可选空格）按以下优先级解析其首个键值对：

1. **空条目有子块**：若内容为空且存在缩进大于当前行的后续子块——
   - **非复杂序列**：子块为该条目的值（条目作为隐式空键 `""` → 值）。
   - **复杂序列**：条目被强制解释为空键映射，键为 `""`，值为子块内容。
2. **空条目无子块**：值为 `null`。
3. **引号开头值**（仅非复杂序列）：按引号值规则（§6.3）解析为字符串。该字符串成为隐式键 `""` 的值。
4. **含结构冒号**（使用 §5 算法）：形成 "键: 值" 对。若值部分为空且有子块，则子块为该键的值；否则按行内值解析。
5. **无冒号纯内容**（仅非复杂序列）：整个内容按标量规则（§12）处理，成为隐式键 `""` 的值。

### 13.3 兄弟键吸收（多键条目核心）

首个键值对解析完毕后，解析器继续扫描后续行，将符合条件的非 `-` 行吸收为同一映射的兄弟键。吸收规则按优先级分为三种路径：

**路径 A — 同级吸收（缩进不变）**：
当前行缩进等于当前条目缩进，且为 KEY_VAL 或 BARE_KEY，则该行被吸收为当前条目的兄弟键。该兄弟键的 key 和 value 按映射规则（§9）解析。此路径持续直到遇到新的 `-` 行或序列块结束。

**路径 B — 子块吸收（缩进增加）**：
当前行缩进大于当前条目缩进（即 INDENT 发生）。若当前条目的首个键值对是"开放键"（有冒号、无行内值），则子块合并为该键的值；否则，子块按以下规则合并到当前条目：

| 父块类型 | 子块类型 | 行为 |
|----------|----------|------|
| 含 `-` 的父块 | 含键的子块 | 子块吸收为父块最后一个 `-` 条目的兄弟键（合并到该条目映射中） |
| 含 `-` 的父块 | 全 `-` 的子块 | 子块合并到父块，作为新的序列条目 |
| 无 `-` 的父块 | 非全 `-` 的子块 | 子块合并为父块的兄弟行（不规则缩进提升） |
| 无 `-` 的父块 | 全 `-` 的子块 | 作为真正嵌套的子块 |

**路径 C — 规则缩进子块**：
当父块最后一个节点为开放键（KEY_VAL/DASH_KEY_VAL/BARE_KEY，无行内值，且已有子内容类型一致），子块直接作为该键的嵌套值。

> **说明**：BARE_KEY 在 INDENT/DEDENT 边界处视为开放键（可吸收缩进子内容为其值），但在同级缩进处不吸收子内容——同级 BARE_KEY 通过路径 A 吸收为兄弟键。

**吸收终止条件**：
- 遇到新的 `-` 行（开始下一个序列条目）。
- 遇到缩进低于当前条目的行（序列或父块结束）。
- 遇到文档分隔符 `---` 或 EOF。

### 13.4 复杂序列模式判定

预扫描整个序列块（直到缩进低于父级或非 `-` 行）。若**任一序列条目满足以下条件之一**，则整个序列为复杂序列：
- 该条目的首个 `-` 行包含结构冒号（使用 §5 算法），即 DASH_KEY_VAL。
- 该条目内容为空，且存在缩进更大的后续有效内容行（即有子块）。

**复杂序列模式的影响**：
- 所有条目统一使用复杂模式解析规则（§13.2 第 1 步覆盖、跳过第 3/5 步）。
- 凡是不含结构冒号且无子块的条目，其内容（去掉 `- ` 后）直接被当作映射键，值强制为 `null`。
- 兄弟键吸收（§13.3）不受复杂/简单模式影响，两种模式均支持多键条目。

### 13.5 示例

**标准多键序列**：
```stml
- 键1: 值1
  键2: 值2
  键3: 值3
- 键4: 值4
  键5: 值5
```
解析为两个序列条目，每个条目是一个多键映射：
```json
[{"键1": "值1", "键2": "值2", "键3": "值3"}, {"键4": "值4", "键5": "值5"}]
```

**不规则缩进多键序列**：
```stml
- 键1: 值1
键2: 值2
 - 键3: 值3
  键4: 值4
```
首行 `- 键1: 值1` 开始条目。第二行 `键2: 值2` 无 `-` 且同级缩进 → 路径 A 吸收为兄弟键。第三行 `- 键3: 值3` 为 `-` 行 → 新条目。第四行 `键4: 值4` → 路径 A 吸收为第二项兄弟键。
```json
[{"键1": "值1", "键2": "值2"}, {"键3": "值3", "键4": "值4"}]
```

---

## 14. 错误恢复与确定性

STML 解析器**必须遵循确定性恢复**，不得 panic，不得基于猜测修改 AST。所有边界情况均按以下规则处理：

- 引号未闭合：降级为原始标量。
- 非法映射条目：按裸键处理（值为 `null`）。
- 块类型冲突：按 §8 结束块并交由父级重新解析。
- 多行字符串未闭合：EOF 自动闭合。
- 内联列表格式错误：降级为原始标量。
- 非法转义序列：保留原样。
- 非根级别的 `---` 行：视为普通内容。
- 任何其他歧义：优先保留原始文本，不进行推断修复。

**确定性声明**：所有符合本规范的 STML 解析器，对于任意输入文本，必须生成完全相同的 AST。

---

## 15. 标准输出（Canonical STML）

将 AST 或 JSON 序列化为 STML 文本时，必须遵守以下规则：

- 所有键和值均用英文双引号包裹。`null` 输出为不带引号的 `null`。内部特殊字符转义（`\n`, `\t`, `\"`, `\\`）。
- 缩进：每级 2 个空格，根级无缩进。
- 列表：只能使用块列表（`- ` 开头）。内联列表仅允许作为块列表条目的值（如 `- ["a", "b"]`），禁止内联列表嵌套（如 `[a, [b, c], d]`）。
- 多行文本：使用 `\n` 转义，禁止 `{…}` 块。
- 键值分隔：冒号后跟一空格，然后写值；若值为复合结构，冒号后直接换行并缩进。
- 序列内联映射：列表元素为映射时，首个键值对以 `- "键": "值"` 格式输出；后续兄弟键以内容缩进（比 `-` 多 2 空格）的 `"键": "值"` 格式输出。多键映射示例：`- "key1": "val1"\n  "key2": "val2"`。若值为复合结构，冒号后直接换行缩进子节点。
- 文档间用 `---` 独占一行分隔。
- **空容器处理**：空映射、空序列与 `null` 在输出中均序列化为 `null`。
- 映射键顺序按 AST 中的顺序输出，保持有序。

---

## 16. 附录

### 16.1 理想结构 EBNF（仅供参考）
以下 EBNF 仅描述理想输入结构，实际解析行为以语义规则和错误恢复规则为准。

```ebnf
file          = doc_set ;
doc_set       = doc { "---" doc } ;
doc           = node ;
node          = mapping | sequence ;
mapping       = { mapping_entry } ;
mapping_entry = quoted_key ":" inline_value
              | unquoted_key ":" [ inline_value | INDENT block DEDENT ]
              | bare_key ;
quoted_key    = '"' { CHAR | ESC } '"' ":" ;
inline_value  = QUOTED_STRING | scalar | inline_list | multiline_string ;
scalar        = UNQUOTED_STRING ;
inline_list   = "[" [ list_element { "," list_element } ] "]" ;
list_element  = QUOTED_STRING | UNQUOTED_STRING ;
block         = mapping_block | sequence_block ;
sequence_block= { sequence_entry } ;
sequence_entry= "-" [ " " ] first_kv { sibling_kv } ;
first_kv     = scalar               (* 无冒号 → 隐式键 "" 的值 *)
             | key ":" [ inline_value ]
             | INDENT block DEDENT ;
sibling_kv   = key ":" [ inline_value | INDENT block DEDENT ]
             | bare_key ;
inline_mapping = key ":" inline_value ;
multiline_string = "{" NEWLINE { ANY_LINE } "}" NEWLINE ;
```

### 16.2 标准输出示例
不规则输入：
```stml
键4:
  - 值41
- 值42
```
标准 STML 输出：
```stml
"键4":
  - "值41"
  - "值42"
```

---

## 待办

- [ ] 支持值为行内字典的转换
- [ ] 支持值为行内列表的嵌套的转换
- [ ] 支持通过“配置”启用/禁止行内列表、行内字典的转换
- [ ] 支持设置不同的键值分隔符
- [ ] 支持设置多文档符号优先级高于多行文本

